import 'package:flutter/material.dart';

class LuckyMode extends StatelessWidget {
  const LuckyMode({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Lucky — TODO')));
}
