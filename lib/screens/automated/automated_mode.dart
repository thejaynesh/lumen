import 'package:flutter/material.dart';

class AutomatedMode extends StatelessWidget {
  const AutomatedMode({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Automated — TODO')));
}
